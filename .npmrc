# FUCO Production System - NPM 配置
# 防止 npm 使用 --force 以避免部署警告

# 禁用 --force 標志
force=false

# 使用 npm registry
registry=https://registry.npmjs.org/

# 設定 Node.js 版本 (與 package.json engines 一致)
engine-strict=true

# 加速安裝
prefer-offline=false
audit-level=moderate

# 生產環境優化
production=false
optional=true

# 日誌等級 (減少輸出)
loglevel=warn

# 快取設定
cache-max=86400000
