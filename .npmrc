# FUCO Production System - NPM 配置
# 現代化的 npm 配置，避免所有 deprecated 選項

# 禁用 --force 標志
force=false

# 使用官方 npm registry
registry=https://registry.npmjs.org/

# 設定 Node.js 版本 (與 package.json engines 一致)
engine-strict=true

# 現代化的安裝偏好設定
prefer-online=true
audit-level=moderate

# 日誌等級 (減少輸出)
loglevel=warn

# 禁用不安全的配置
fund=false
